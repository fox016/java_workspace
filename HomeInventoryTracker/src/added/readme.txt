This is just used for plugin development.

Note that any plugins created here will not actually be incorporated by the plugin manager!

Instead, compile the program, then take the .class files generated by the compiler
and put them into the plugin directory.